/* Deprecated: the dedicated "Busca" tab was folded into the gallery.
   Search, image search, "similar" and random now live in gallery.js and render
   into the gallery grid. This module is no longer imported and is kept only as a
   tombstone (the environment doesn't allow deleting files). Safe to remove. */
